import { FakeFeedConfig } from 'shared/lib/types';

export const fakerConfig: FakeFeedConfig = {
    // feedCount: 100_000_000,
    // usersCount: 100_000,
    feedCount: 100,
    usersCount: 10,
};

export const RESPONSE_DELAY = 300;
